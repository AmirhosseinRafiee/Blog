version: "3.9"

services:
  
  backend:
    build: .
    container_name: backend
    volumes:
      - ./mysite:/app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
